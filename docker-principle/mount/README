1. add `// +build linux ` before package main it will stop goland error hint

2. build with `GOOS=linux go build -v` rather than go build

https://here2say.com/39/

# 下载 rootfs
wget http://dl-cdn.alpinelinux.org/alpine/v3.12/releases/x86_64/alpine-minirootfs-3.12.1-x86_64.tar.gz